 doctype html
html
  head
    meta(charset="utf-8")
    title Reset your password
    style.
      body { font-family: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Oxygen,
              Ubuntu, Cantarell, Fira Sans, Droid Sans, Helvetica Neue, sans-serif; }
      .card { max-width: 520px; margin: 20px auto; padding: 24px; border: 1px solid #eee; border-radius: 8px; }
      .btn { display: inline-block; padding: 10px 16px; background: #162c74; color: #fff; text-decoration: none; border-radius: 4px; }
  body
    .card
      if username
        h2 Hello #{username},
      else
        h2 Hello,
      p We received a request to reset your PedBook password.
      p Click the button below to choose a new password:
      p
        a.btn(href=resetUrl target="_blank" rel="noopener") Reset Password
      p If you did not request this, please ignore this email.
      p This link will expire soon for your security.
      p Regards,
      p The PedBook Team
